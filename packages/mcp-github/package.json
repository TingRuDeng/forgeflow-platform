{
  "name": "@forgeflow/mcp-github",
  "version": "0.1.0",
  "private": true,
  "description": "Thin MCP wrapper exposing GitHub tools (create_branch, open_pull_request, get_pull_request_status). Delegates to dispatcher GitHub integration via deps. Behavior is intentionally limited to the tool definitions.",
  "type": "module",
  "exports": {
    ".": "./src/server.ts"
  },
  "keywords": [
    "mcp",
    "thin-wrapper",
    "github"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  }
}
